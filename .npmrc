# pnpm configuration
auto-install-peers=true

# In production environments (like Vercel), these build scripts 
# are handled automatically and the warnings can be safely ignored
# The packages listed (@tailwindcss/oxide, sharp, unrs-resolver) 
# are legitimate dependencies that work correctly without manual script approval